package Models

import java.util.Date

class Announcement(id: Int, username: String, title:String, content: String, date: Date) {
    val id = id
    val username = username
    val title = title
    val content = content
    val date = date
}